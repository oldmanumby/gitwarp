{
  "name": "gitwarp-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "sync": "git fetch && git merge origin/main && git push origin dev"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@secretlint/secretlint-rule-preset-recommend": "^13.0.4",
    "eslint": "^10.7.0",
    "fallow": "^3.7.1",
    "husky": "^9.1.7",
    "prettier": "^3.9.6",
    "secretlint": "^13.0.4"
  },
  "version": "1.4.0"
}
